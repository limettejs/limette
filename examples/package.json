{
  "name": "limette-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test:routes": "vite --config vite.config.ts build --ssr ../tests/smoke/vite-routes.ts --outDir .vite-limette-smoke",
    "start": "node main.js",
    "start:deno": "deno run -A main.js"
  },
  "dependencies": {
    "@limette/core": "file:..",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.0"
  }
}
